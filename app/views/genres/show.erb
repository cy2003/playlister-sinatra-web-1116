<h2><%= @genre.name %></h2>


<h3>Genre's Artists</h3>
<ul>
  <% @genre.artists.each do |artist| %>
  <li>
    <a href="/artists/<%= artist.slug %>"><%= artist.name %>
  </li>
  <% end %>
</ul>

<h3>Genre's Songs</h3>
<ul>
  <% @genre.songs.each do |song| %>
  <li>
    <a href="/songs/<%= song.slug %>"><%= song.name %>
  </li>
  <% end %>
</ul>
